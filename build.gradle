plugins {
    id 'java'
    id 'pmd'
}

repositories {
    jcenter()
}

pmd {
    toolVersion = "6.39.0"
    ruleSets = []
    ruleSetFiles = files("pmd.xml")
}


dependencies {
    implementation files(fileTree(dir: 'jar_files', includes: ['*.jar']))
}